Project Structure

smart_query_analyzer/
├── app.py            ← Streamlit UI (main entry point)
├── database.py       ← MYSQL setup, sample data, schema info
├── nl2sql.py         ← Rule-based Natural Language → SQL converter
├── executor.py       ← Query runner + optimization analyzer
├── requirements.txt  ← Python dependencies
└── README.md
